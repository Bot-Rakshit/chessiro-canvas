Default bundled piece assets
============================

The bundled default piece artwork in `assets/pieces/chessiro/*.svg` is generated
from `react-chessboard` default piece renderers by this script:

- `scripts/generate-default-piece-svgs.mjs`

`react-chessboard` is MIT-licensed, and these generated defaults are distributed
with this package under MIT as well.
